\begin{landscape}
\begin{tiny}
\section{Schéma fonctionnel}
\begin{parsetree}
    ( .{Pourquoi les joueurs se plaisent-ils sur \SL ?}. 
		( .{Recherche d'informations}.
           `Anglophonie'
            (.{Trouver les personnes}.    `Points clés/coordonnées' )
            (.{Objectifs modifiables}. 
				(.{AIML}.  ~`Aides : configuration')
            )
         )    
         (.{Interprétation des résultats}.  `Historique'  `Statistiques')
         ( .{Humanité}. `Gestion des erreurs'  `Personnalité' `Temps de réponse')
    )
\end{parsetree}
\end{tiny}
\paragraph{Remarque : } Les fils sont les conditions nécessaires à un fonctionnement optimal du père, cependant, certaines ne sont que des améliorations possibles (et donc pas indispensables). Pour chaque niveau, la fonction la plus à gauche est la plus importante.
\end{landscape}